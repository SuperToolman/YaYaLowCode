# 更新日志

本文档用于记录项目的重要变更。

## 1.10a - 2026-08-25 至 2026-09-02

### 新增

- 引入 DeepSeek Harness（DSH）作为 Agent 运行基础，新增 YaYa Platform 扩展、YaYa Host 启动入口、Web profile 配置及 DSH 集成文档。
- 新增 DSH 与 YaYa 平台的会话绑定、租户/用户身份快照、请求 ID、内部共享密钥和运行时审计链路；Agent 工具统一经 Rust Platform API 访问业务数据。
- 新增应用级 Agent 工具，支持查询应用、表单、字段大纲、导航、关联记录、自动化和工作流运行态，并提供通用表单 Schema 合同读取能力。
- 新增 Agent 事务接口，覆盖事务创建、查询、执行、取消和高风险操作审批；扩展应用、表单、自动化、导航分组、明细表及 Schema 草稿等写入动作。
- 新增 Agent 文件与产物能力：会话文件上传、列表、下载、删除、文件链接、运行时输出登记及输入/输出类型区分；新增文件、链接和工作区存储迁移。
- 新增租户/用户范围的自有模型路由 API，支持供应商增删改查、默认模型、模型列表、启停状态、密钥加密保存及运行时路由解析。
- 新增应用市场首版发布申请与同步能力，支持应用快照、Schema/存储定义打包、提交状态查询及从运营端同步已审核应用版本。
- 新增 Agent 独立工作台页面、AI 员工选择、会话列表与重命名、消息/思考/工具调用展示、文件上传和产物下载。
- 新增设置偏好页面和主题系统，支持明暗/跟随系统、主题预设、强调色、背景/表面、圆角、字体和动画偏好持久化。
- 新增表单侧栏导航分组创建、重命名、移动、删除及拖拽排序；应用首页、表单运行页、设计器和记录表补充新的响应式布局与组件。
- 新增应用管理和市场提交入口、Agent 相关 BFF 代理路由、模型路由 BFF、文件/产物/事务/审批路由及相应 OpenAPI SDK 类型。
- 新增 AI 员工运行时定义解析：从签名许可证和本地安装快照生成 Persona、Skills、工具白名单、应用范围和系统提示词，并暴露运行时员工列表与头像代理。
- 新增技能资源 manifest 生成、资源冲突检测和缓存版本刷新，确保许可证更新后 DSH Host 使用最新的员工能力边界。
- 新增应用业务上下文、字段大纲、表单关系、明细表、导航和工作流工具的统一查询入口，补齐 Agent 设计表单所需的结构化上下文。
- 新增应用创建、编辑、暂停、删除和创建者归属字段；新增线上应用版本、发布状态和市场提交状态字段，并补充应用快照所需的导航、表单、Schema 和存储定义信息。
- 新增 Agent workspace 配置项，支持工作区根目录、最大字节数、最大文件数和保留天数的环境变量控制；本地默认落在仓库 `runtime/agent-workspaces`。
- 新增 `AgentPanel`、表单页面视图配置、表格设置和系统页面组件；表单运行页支持按视图切换、字段大纲弹窗和新的记录操作入口。
- 新增通用 `PageContentLayout`、`SettingsPage`、`CardGroup`、`MySurface`、`MyTable`、头像、颜色选择器、信息提示和选项菜单等基础 UI 组件。
- 新增消息页、日志页、通知页、关于页的独立样式模块，以及主题设置页；统一支持暗色模式、移动端布局、键盘操作、焦点样式和可访问性标签。
- 新增桌面端构建、仅跳过 DSH 构建的开发启动和独立 Web 发布脚本，便于本地调试、Tauri 打包和分离式部署。

### 变更

- Agent 运行时从仓库内置 Cordis Runtime/LangGraph Worker 改为外部 DSH Harness + YaYa plugin；删除旧 `agent/` Runtime、插件组合和 LangGraph Worker，开发与检查命令切换到 `agent/deepseek-harness`。
- Agent 写操作由旧 pending action 命名统一迁移为 transaction；删除旧 pending-action BFF，改用 approvals/transactions 路由，并由 Rust 信任边界再次校验 AI 员工工具白名单和 RBAC。
- Agent 会话删除改为归档而非物理删除，列表仅返回 active 会话；会话绑定真实 AI 员工 ID，并新增归档时间和恢复所需的数据结构。
- 客户模型配置改为按租户和用户隔离的自有模型路由；模型密钥使用 AES-GCM 加密，运行时通过受控内部接口读取，前端和许可证不再接触明文密钥。
- 部署拓扑调整为 Web/Rust API 与单独 DSH Harness；Compose、单容器、Supervisor、发布脚本和本地启动脚本同步 DSH 环境变量、共享密钥、工作区配额及 `agent-state` 卷配置。
- 应用、表单、导航及设置 API 按新的领域 DTO、授权检查和运行时身份契约整理，重新导出 OpenAPI 与前端 SDK。
- 表单创建、Schema 保存、导航移动和自动化写入统一通过事务执行；Schema 草稿写入要求完整的标准字段布局，不再接受旧的简化字段格式。
- 表单 Schema 保存改为直接应用当前版本并同步记录存储计划；移除独立发布接口，草稿/已发布版本语义统一为当前 Schema 版本。
- 应用列表、详情、更新和删除按创建者、应用管理员及 `app:<id>:display` 权限重新过滤；暂停应用对普通用户隐藏，但创建者和管理员仍可管理。
- 平台租户解析增加主组织与组织成员关系回退逻辑；运行时身份和模型路由均强制要求有效租户范围。
- 应用删除、表单删除和回收站流程改为显式状态与权限判断；应用管理员可查看暂停应用，普通成员只看到启用且拥有展示权限的应用。
- 表单运行时统一使用 `current_schema_version`，同步调整记录表、表单视图、动态存储定义、详情表和首屏 bootstrap 的版本读取逻辑。
- Agent 平台工具目录新增创建/编辑/删除应用、读取字段大纲、读取 Schema 合同等能力，并为每项工具标记分类、读写风险等级和授权要求。
- 会话、事务、文件和模型路由的 BFF 统一转发认证头与错误响应；删除旧的 Agent 配置、发布和 pending-action 路由，避免前端继续依赖已退役 API。
- `agent-runtime-proxy`、后端 JSON proxy、文件上传和 RUM 路由调整内部请求处理；BFF 不再回退到旧 Rust Agent 执行器。
- 模型路由由单一全局默认值改为租户/用户隔离；同一租户仅允许一个默认路由，更新或删除默认路由时重新计算可用状态。
- 应用市场同步落盘到平台发布目录，并在批准版本同步时更新应用线上版本和 release ID；未批准或同步失败的提交不会覆盖现有应用。
- 设置导航重新分组为平台设置、权限中心、AI 员工、通讯等工作区；数据库连接仅对本地部署显示，模型设置改为“自有模型”。
- 表单侧栏导航项支持分组展开状态持久化、拖拽移动、搜索过滤、创建/重命名/删除分组和表单重命名后的事件同步。

### 优化

- DSH Host 在会话开始前注入 YaYa 上下文，按 AI 员工动态过滤工具和平台权限；高风险删除操作才弹出人工确认，普通创建/编辑操作在 RBAC 允许时直接执行。
- Agent SSE 兼容 DSH session event、工具事件、事务状态和 artifact.created，消息历史、刷新恢复、会话标题和产物列表保持一致。
- Agent 工作区增加文件名清理、危险扩展名拒绝、单文件/工作区大小和文件数限制、校验和记录以及按用户/员工/会话隔离的路径约束。
- 应用市场发布使用完整应用快照并生成可下载压缩包，发布同步失败保留本地提交记录，避免影响客户侧数据。
- 记录表改用 TanStack Table/Virtual，表单运行页和设计器侧栏采用更细粒度组件与固定尺寸布局；统一应用壳、设置页、消息页、日志页和通知页的视觉与响应式交互。
- 启动脚本支持跳过 DSH 构建、桌面端构建和 Web 单独发布；API 健康检查、OpenAPI 导出及依赖安装流程同步更新。
- OpenAPI/SDK 生成结果同步覆盖新的应用市场、运行时员工、Schema 合同、事务、文件和模型路由接口；前端 API 客户端移除已删除的旧发布和 pending-action 方法。
- 表单运行时兼容历史 Agent 生成的紧凑字段结构，在读取时规范化 `fieldId/fieldName`、顶层 `required/options` 及缺失布局属性，避免旧数据无法打开。
- 记录表支持服务端查询结果的分页、排序、筛选、虚拟滚动和列配置；空状态、加载状态、错误状态及窄屏列布局统一处理。
- 设计器工作台重新整理画布、属性抽屉、字段属性布局、组件工具和顶部操作区，减少重复渲染并改善字段编辑、分组和 Schema 预览的交互。
- 应用首页和侧栏改为卡片化、响应式布局，补充应用统计、状态标签、线上版本、记录数量和应用快捷操作；支持创建应用后立即进入编辑。
- Agent 消息 Markdown、思考块、工具调用卡片和消息编辑器统一 DSH 事件模型，改善代码块、错误提示、滚动定位和文件待发送列表展示。
- 主题系统通过 CSS 变量生成明暗表面、强调色和层级背景，支持预设主题、系统主题监听、字体/圆角/动画设置以及刷新后的偏好恢复。
- 登录页、应用壳、表单运行页、设置页和弹窗统一使用新的 surface、border、shadow 和 modal 样式，降低不同页面之间的视觉差异。
- 依赖与锁文件同步升级，加入 `@tanstack/react-table`、AES-GCM/base64 Rust 依赖及 DSH workspace 依赖，确保构建和类型生成使用一致版本。

### 修复

- 修复 Agent 会话跨用户/跨租户访问、恢复时身份丢失以及工具绕过 AI 员工授权的问题；内部身份请求头现在只接受共享密钥保护的 Host 请求。
- 修复事务重复执行、过期事务仍可写入、删除操作缺少二次授权和自动化定义节点/边丢失的问题。
- 修复文件输出被当作输入、任意路径访问、会话文件越权下载及文件链接角色不受限的问题。
- 修复 BYOM 默认路由全局串用、模型密钥明文保存和非本地部署许可证错误限制自有模型的问题。
- 修复应用、表单、导航分组和设计器在重命名、移动、刷新后状态不同步，以及记录表、侧栏和设置页在窄屏下溢出的问题。
- 修复表单草稿/已发布版本混用导致的首屏数据、Schema 回滚和存储定义不同步；无变更保存会复用当前版本，避免无意义地递增版本号。
- 修复旧账号缺少主组织字段时无法访问运行时接口的问题，并统一管理员、创建者和资源级权限的应用可见性判断。
- 修复 AI 员工 Skill 与已有资源同 ID 时静默覆盖的问题，现在会拒绝冲突并保留平台已有资源。
- 修复应用创建者无法访问自己暂停应用、管理员无法管理非启用应用及应用删除缺少所有权校验的问题。
- 修复详情表、定义表单和普通表单在 Schema 读取、发布状态和存储计划同步上的分支不一致问题。
- 修复 Agent 生成的旧字段别名导致表单渲染崩溃、字段布局缺失、必填和选项属性丢失的问题。
- 修复导航分组删除后子项、表单移动后侧栏展开状态以及拖拽排序后的本地/服务端顺序不一致问题。
- 修复模型供应商编辑时 API Key 被清空、默认模型不在模型列表、禁用供应商仍被运行时选中的问题。
- 修复上传文件路径穿越、非法文件名、危险脚本扩展名、超过工作区配额和重复登记输出文件的问题。
- 修复 Agent SSE 断线后残留“正在思考”、artifact 重复显示、历史消息文件无法关联和会话标题刷新丢失的问题。
- 修复设置页、消息页、日志页和通知页在暗色主题及小屏幕下的对比度、滚动容器和内容溢出问题。
- 修复 OpenAPI 重新生成后 SDK 类型与 Rust 路由不一致，以及 Next.js 路由仍引用已删除 handler 导致的构建错误。

### 破坏性变更与迁移要求

- **旧 Cordis/LangGraph Agent 已删除。** 部署必须单独提供 DSH Harness，并配置 `DSH_HOST`、`DSH_PORT`、`DSH_PROFILE`、`DSH_HOME`、`YAYA_PLATFORM_API_URL` 和 `AGENT_RUNTIME_SHARED_SECRET`。
- **pending-actions 接口已退役。** 集成方必须迁移到 `/api/agent/sessions/{sessionId}/transactions` 及 approvals BFF；事务状态以 `pending/executing/completed/cancelled/failed/expired` 为准。
- **Agent 会话删除语义改变。** 删除仅归档会话，生产备份和清理任务需同时处理 `archived_at` 与 Agent 文件工作区。
- **自有模型需要新的密钥配置。** 部署必须设置 Base64 编码的 32 字节 `YAYA_BYOM_ENCRYPTION_KEY`，并按租户/用户迁移现有模型配置。
- **Compose 不再启动内置 Agent/Worker 服务。** 旧 `yaya-agent`、`yaya-langgraph-worker` 配置、模型/Embedding 环境变量和 8790 Worker 依赖应从部署环境移除，DSH Harness 由独立 profile 管理。
- **表单发布接口已移除。** 原 `/api/forms/{formUuid}/publish` 及对应 SDK 调用方应改为保存 Schema 后直接使用当前版本；读取 `draft/published` 版本时统一以 current schema 为准。
- **Agent 员工能力来源已收敛。** 客户端不得再通过请求体编辑 Persona、Skills、系统提示词、工具白名单或应用范围；这些字段只来自运营端签名许可证和本地安装快照。
- **数据库迁移必须执行到 2026-09-02。** 迁移包含 `agent_sessions.archived_at`、`agent_files`、`agent_file_links`、BYOM 租户/创建者范围和应用市场字段；执行前应备份平台数据库及 `runtime/agent-workspaces` 文件目录。
- **应用市场为首版发布快照能力。** 当前只支持应用快照提交、运营端审核同步和版本标识，不代表客户平台已经具备完整的公开应用分发、订单结算或在线安装闭环。

## 1.09a - 2026-08-24

### 新增

- 新增 YaYa Agent Host，接入 DSH Plugin/Service、租户身份快照和 BYOM 动态模型路由。
- 新增低代码表单、自动化、工作流工具及 Rust 审批、审计链路。
- 新增 DSH 原生会话恢复、SSE timeline 和 `/agent` 页面会话流程。

### 变更

- Agent 运行时从旧 Runtime/LangGraph/旧 Rust runner 切换为 DSH/Cordis，旧 Agent 实现退出运行链路。
- 请求链路固定为浏览器 → Next.js BFF → DSH Host → YaYa Platform Plugin → Rust API；模型和工具不得直接访问平台数据库。
- AI 员工包统一承载 Skill、Plugin 和工具白名单；人格不再参与新执行链。
- 模型配置改为租户自有模型和默认路由，会话由平台记录与 DSH session 双边管理。

### 优化

- 按 DSH 事件顺序展示分析、工具、结果和回答，工具权限按 AI 员工动态隔离。
- 新建会话延迟到首条消息，标题持久化；一键启动脚本统一启动 Web、Rust API 和 DSH Host。

### 修复

- 修复会话历史为空、刷新丢失、Host 重启无法恢复和旧工具记录重复的问题。
- 修复新建会话串线、标题不持久化以及“正在思考”无法结束的问题。
- 修复认证、租户身份和空/非法 JSON 导致的 Agent 运行失败；Host/Web 类型检查及首轮、续轮、刷新恢复回测通过。

## 1.08a - 2026-08-19

### 新增

- AI 员工安装包现在可由 Cordis Runtime 通过受认证的 Rust 平台接口读取；新会话绑定具体员工 ID，并在执行时加载运营端发布的 Persona、Skills 与工具授权边界。
- 新增租户级“自有模型”设置、加密存储和默认路由；所有 SaaS 与本地部署客户均可管理自己的 OpenAI-compatible 模型供应商。

### 变更

- 客户侧 AI 员工安装不再导入 Skill ZIP、创建 Rust Agent Definition 或模型 Profile。安装仅保存授权快照，Agent 能力组合由 Cordis 插件服务在运行时解析。
- 客户侧 `/api/settings/ai-employees/configurations*` 及对应 Next.js BFF 已删除；客户不能再选择员工模型、编辑系统提示词、Skills 或工具配置。
- Cordis `model-openai-compatible` 改为通过受内部共享凭据保护的运行时接口解析租户默认模型；模型密钥不再来自 Agent 环境变量或浏览器请求。

### 优化

- AI 员工选择器仅展示当前已安装且许可证有效的员工；员工会话与平台 backing session 均保存相同的员工 ID。
- 针对 AI 员工的工具调用增加运行时许可校验，运营端未授权的工具即使被模型提议也会被策略插件拒绝。

### 修复

- 修复 AI 员工安装状态与 Cordis 实际运行能力脱节的问题，移除旧 Rust Agent 配置同步作为运行时依赖。
- 修复将客户自有模型误写为本地部署许可证能力的产品边界；它现在只受租户管理员权限和后端密钥保护约束。

## 1.07a - 2026-08-18

### 新增

- 新增独立部署的 Cordis Agent Runtime，提供会话、消息流、工具、策略审批、记忆、审计、沙盒、调度和 UI 协议适配等插件服务。
- 新增 `agent/` 独立工作区、开发/生产插件组合配置、Docker 镜像和 LangGraph Worker 镜像；根工作区增加 Agent 检查与启动命令。
- 新增 Agent 插件 manifest 与配置校验机制，支持声明 `id`、版本、提供服务、依赖、能力与配置 schema，并拒绝重复服务提供者和缺失依赖。
- 新增 Agent run、步骤、审批 checkpoint 的 JSON/PostgreSQL 存储实现；待审批动作确认后可恢复同一 run。
- 新增 `agent_runtime_sessions`、消息、run、步骤、checkpoint 和记忆独立存储模型，避免 Agent Runtime 直接写入平台领域表。
- 新增 Cordis Agent BFF 代理、SSE 的 `run.paused` 事件解析、待审批展示及确认/取消后的恢复链路。
- 新增平台 `runtime-identity` 身份契约，Agent 使用稳定 tenant/user/权限快照归属会话，令牌刷新不再切断会话可见性。
- 新增 LangGraph Worker 客户端协议与 `8790` 默认运行端口，为独立 Worker provider 预留可替换 workflow 边界。
- 新增生产插件组合：PostgreSQL storage、pg-boss 持久化调度、Docker sandbox 和外部 LangGraph workflow client 均可由配置选择。
- 许可证契约新增 `pluginPackages` 权益结构，包含插件包 ID、版本、哈希、入口模块、manifest 与到期时间；该权益不包含任何凭据，为后续运营端插件商品化和客户侧验签安装提供协议基础。

### 变更

- 移除 Rust 旧 Agent runner、工具执行和会话消息运行时；Rust 平台保留身份校验、RBAC、会话 backing record、审批状态机与最终领域写入事务，Agent 推理与工具循环迁移至 Cordis Runtime。
- Rust 的 `agent_message`、`agent_run`、`agent_run_step` 等旧运行时实体及旧 Agent capability/plugin/tool 模块已删除；平台 API 不再承担模型循环或 SSE 推理。
- Agent 会话前端改为统一经由 Cordis BFF 访问，不再依赖会被 OpenAPI 生成覆盖的旧 Rust Agent SDK 导出。
- Agent HTTP 协议改为 BFF -> Cordis Runtime -> Rust Platform：浏览器不再直接使用旧 Rust `/api/agent/*` 运行接口；Runtime 不可用时 BFF 返回 `503`，不再回退到旧执行器。
- 客户侧设置下架“模型供应商”和“插件”入口；旧直达页面重定向至 AI 员工市场，AI 员工市场不再允许客户为员工选择模型供应商或模型。
- 移除客户侧 `/api/agent/providers*` 与 `/api/agent/plugins*` 的公开 CRUD 路由和对应 BFF，旧配置数据仅保留为迁移兼容，不再作为 Cordis 运行时配置来源。
- Cordis OpenAI 兼容模型插件继续仅从受控运行环境读取模型地址、密钥和模型名称，避免客户侧页面配置与真实 Runtime 脱节。
- AI 员工、人格、Skill、工具白名单和系统提示词继续由运营端封装；客户平台只保留市场安装、更新、移除和会话使用入口。
- 本地开发与部署端口统一为 Web `8787`、Rust API `8788`、Cordis Agent `8789`、LangGraph Worker `8790`；同步更新 Compose、镜像、Supervisor、Tauri、发布和数据库迁移脚本。

### 优化

- 一键启动脚本增加对遗留 Next.js 开发进程和端口占用进程的识别与清理，并在 API、Agent 健康检查通过后再启动前端。
- 单容器与 Compose 部署链路使用统一端口映射和健康检查，发布后的初始登录校验与数据库恢复校验同步切换到新端口。
- Agent 的会话更新和删除同时同步平台 backing record 与 Runtime 存储，保持客户会话、审批和审计边界一致。
- Agent 记忆检索继续按租户、用户和路由作用域隔离，并保留脱敏、保留期、删除权和嵌入版本重建边界。
- 工具循环改用模型原生 `tool_calls` 协议，增加最大迭代次数、run 超时、取消传播、token 预算、模型/工具步骤审计和可恢复执行边界。
- 沙盒改为默认拒绝执行；启用后通过一次性 Docker 容器施加只读根文件系统、工作目录挂载、网络、CPU、内存、PID 和时长约束。
- 开发/生产配置分离：本地 JSON storage 仅用于开发，生产路径切换为 PostgreSQL、持久化队列和受限 Worker 通信。

### 修复

- 修复启动时 OpenAPI SDK 重新生成后前端仍导入已删除 `listAgentMessages`，导致首页编译失败的问题。
- 修复新版 Agent 会话缺少更新、删除 BFF 路由的问题，并补齐 Cordis 网关及 JSON/PostgreSQL 存储实现。
- 修复前端已有 Next.js 开发服务器时一键启动因 `.next/dev` 锁冲突而终止 API 与 Agent 进程的问题。
- 修复单容器 Supervisor 将 API 误绑定到旧 Web 端口的问题。

### 破坏性变更与迁移要求

- **旧 Rust Agent API 已退役。** 任何直接调用旧 Rust Agent 消息流、run trace、可用 Agent、工具或 runner 接口的客户端/集成，必须迁移到 Next.js `/api/agent/*` BFF 和 Cordis Runtime 协议。
- **不再提供 Rust Agent 回退。** 部署升级前必须同时部署 `yaya-agent`；生产环境还应配置模型、embedding、Worker 内部令牌及 Agent PostgreSQL 连接。Agent 不可用会明确返回 `503`。
- **旧客户侧模型供应商与插件配置已下架。** `/api/agent/providers*`、`/api/agent/plugins*` 不再可用；客户模型迁移至独立的租户“自有模型”路由，插件仍由运营端商品和许可证下发。
- **端口已整体迁移。** 原 Web `3000`、API `8787`、Agent `8790` 的开发、反向代理、健康检查、Tauri 地址和运维脚本必须改为 `8787/8788/8789`；Worker 使用 `8790`。
- **数据备份范围扩大。** 除平台 PostgreSQL 与 `api-state` 外，生产环境必须备份 `agent_runtime_*` 表；升级前应验证 transaction、checkpoint 和已安装 AI 员工包可恢复。
- **插件商品化尚未完成。** `pluginPackages` 目前仅为许可证兼容字段，运营端尚未提供插件商品目录、订单绑定、签名包下载和客户侧校验安装，不能据此宣称已支持外部插件市场。

## 1.06a - 2026-08-07

### AI 员工版本同步

- AI 员工列表卡片调整为响应式三列布局，并同时显示当前安装版本与运营端最新版本。
- 列表明确使用当前包版本和最新包版本进行展示与更新判断；卡片收敛为紧凑信息布局，仅在版本不一致时显示更新按钮。
- 修复 AI 员工更新只写入本地安装包、未同步数据库 Agent/Profile 的问题；现在会同步员工名称、系统提示词、Skills 和模型配置名称。
- 新增 AI 员工移除接口和列表操作，移除时禁用运行时 Agent、删除本地安装包，重新安装后可恢复运行。
- 员工自身配置、绑定人格或任一 Skill 发生变化时，运营端分别迭代资源版本并重新计算 AI 员工包版本。
- 包版本按员工、人格和全部关联 Skills 的版本号逐位累加，平台端只使用包版本进行更新检测。
- 平台端在已安装员工检测到版本差异时显示“更新”按钮；同步请求携带当前许可证，由运营端返回最新完整安装包。
- 更新会覆盖本地人格、Skills、系统提示词和模板版本，但保留客户侧模型供应商配置。

### 流程任务工作台

- 将应用内分散的待办、已处理、已创建和抄送页面合并为独立的“我的任务”工作台，支持跨应用查看、搜索和处理流程任务。
- 任务入口同步加入账户菜单和应用侧栏；保留旧系统页路径兼容，并允许从应用侧栏按当前应用筛选任务。

### 客户侧 AI 员工配置收敛

- 移除设置中的通用 Agent 分组、Agent/配置档案/人格/Skills 管理页面及对应客户 API 路由。
- 新增 AI 员工专用配置接口；客户仅可为已授权且已安装的 AI 员工选择模型供应商和对话模型。
- AI 员工的人格、Skills、系统提示词、执行参数和工具白名单由运营端封装，作为安装包内部运行结构保存。
- AI 员工分组保留 AI 员工列表、AI 员工市场、模型供应商、知识库和插件入口；权限页同步按已安装 AI 员工生成使用权限。
- 重新生成 OpenAPI 与前端 SDK，旧 Agent 配置接口不再对外暴露。

### 系统 AI 与运行资源

- 移除 Agent 协助设置及其持久化配置；Schema 分析、Agent 填表等系统 AI 能力统一读取“默认模型供应商”。
- 模型供应商新增“是否默认”属性；首个供应商必须设为默认，系统 AI 在没有默认供应商时不提供启用入口。
- 普通 Agent 会话不再依赖系统默认 Agent，必须明确选择已安装的 AI 员工。
- 客户侧资源管理仅保留知识库和插件；人格、Skills、工具授权及 ZIP 技能包管理收敛到运营端，底层安装结构继续保留。

### AI 员工购买闭环

- 公共支付弹窗的测试支付会调用平台后端，不再使用浏览器会话伪造购买和安装状态。
- 平台以当前许可证向运营端提交 AI 员工购买请求，由运营端根据稳定主体识别客户并返回订单与新许可证信息。
- 平台启动及许可证状态检查会提示存在新签名，不再静默覆盖本地签名；拥有许可证管理权限的用户可确认更新。
- 确认更新后，AI 员工市场根据新许可证显示“已经拥有，未安装”，用户安装后再变为“已安装”。
- AI 员工安装会将许可证内的人格、Skills、系统提示词和模板版本完整保存为本地安装包，供应商、知识库与插件仍由客户配置。

### 部署类型与平台设置

- 许可证新增 SaaS/本地部署类型，并在“关于平台”展示当前部署模式。
- 数据库连接菜单仅对本地部署许可证显示；设置首页默认进入“关于平台”，避免 SaaS 用户进入不可用页面。
- 数据库和 Valkey 配置接口同时校验部署类型，SaaS 许可证即使直接请求接口也会被拒绝。

### AI 员工市场

- AI 员工市场从运营管理平台实时读取已上架商品，展示名称、分类、版本、价格和计费周期。
- 市场根据当前签名权益区分“未购买”“已经拥有，未安装”和“已安装”，许可证更新后自动刷新。
- 已拥有的 AI 员工可安装到当前平台，安装状态由平台后端持久化；AI 员工列表只展示已安装项目。
- 未购买商品使用公共支付模态框，并展示当前商品、金额和计费方式。

## 1.02a - 2026-08-06

### 许可证与平台授权

- 平台在远程校验许可证时，会按稳定授权主体自动发现并同步最新的已签发许可证。
- 自动同步前会重新验证 RS256 签名、签发方、受众和主体一致性；同步后自动通知运营管理平台完成激活。
- 新增模块或 AI 员工后，客户无需再次手动粘贴签名，后续访问受保护业务接口即可获得最新授权。
- 首次配置、主体异常或当前许可证已过期/销毁时，仍可通过“更新签名”入口手动恢复。

### 平台设置与授权展示

- “关于平台”同时展示平台、通讯模型等模块的独立有效期，并优先使用许可证中的 `title` 展示模块名称。
- 许可证状态响应补充模块状态、模块到期时间、AI 员工授权、AI 员工状态和授权主体信息。
- 许可证更新后通过 `yaya-license-updated` 事件刷新应用壳、关于平台和 AI 员工列表，避免页面继续显示旧授权。
- AI 员工列表与 AI 员工市场使用新建的设置结构，客户平台仅配置供应商、知识库和插件等运行时资源。

### Agent 交互

- Agent 消息弹窗头部下方增加横向机器人列表，可在当前会话入口直接切换机器人。
- 机器人切换保留现有会话边界和模型配置，不再要求用户离开消息弹窗进入设置页操作。
- 设置导航增加“AI 员工”一级分组，包含“AI 员工列表”和“AI 员工市场”；既有机器人配置架构继续保留。

### 兼容与验证

- 继续兼容 `yaya-license-center` 历史 issuer 和 `LICENSE_CENTER_*` 配置，新的运营端使用 `yaya-operation-center` 命名。
- 平台端 Rust 检查和 35 项后端测试通过；许可证自动同步会拒绝主体不一致的最新签名。

## 1.01a - 2026-08-05

> `1.01a` 为部署迁移与本地运行维护版本；`0.99a` 中的前端性能、领域化重构、表单查询与 Agent 表单工具变更仍归属 `0.99a`。npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增远程数据库巡检脚本，可通过 SSH 汇总表单存储定义、动态数据表、Agent Skill 关联及运行时 Skill、上传文件数量，不修改远程数据。
- 新增本地数据库整体迁移脚本：在显式确认后，导出本地 PostgreSQL 数据库、同步 Skill 包与上传文件，先备份远端数据库，再替换远端 `public` Schema 并等待 API 健康检查恢复。迁移脚本会移除 PostgreSQL 18 专有的 `transaction_timeout` 设置，以兼容当前 PostgreSQL 15 部署镜像。

### 调整

- 单容器部署配置移除已废弃的 Agent 注册表和 RBAC 文件路径，运行时设置统一使用数据库持久化。
- 本地开发启动脚本默认读取 `deploy/secrets/license-public.pem`，启动前会终止已有后端进程树以避免运行旧代码；可通过 `-ReuseBackend` 保留现有健康后端。

### 修复

- 修复单容器镜像构建时 OpenAPI 导出使用构建目录二进制，以及运行时复制无效 Next SWC 缓存的问题。

### 配套项目：运营管理平台（`yaya-operation-center`）

- 新增订单中心、客户管理、财务管理、许可证签发和 AI 员工管理五个运营工作区，并将管理台首页重定向到订单中心。
- 新增客户档案及启用/停用管理；新增订单创建、商品明细、预计回款日期、订单取消和回款登记，回款支持分笔记录，订单足额到账后才转为“已回款”。
- 新增财务汇总与收款流水查询，提供累计回款、本月回款、待收金额、已回款订单数和活跃客户数。
- 新增 AI 员工商品目录，可维护商品名称、分类、价格、计费周期和上下架状态，并将商品作为订单和许可证授权来源。
- 许可证签发支持关联订单；关联订单必须已足额回款。许可证载荷和管理台详情新增订单关联及 AI 员工权益、独立有效期和状态展示。
- SQLite 自动创建并迁移客户、订单、交易、AI 员工商品及许可证扩展字段，保留既有许可证和旧版 JSON 导入兼容能力。

## 0.99a - 2026-08-04

> `0.99a` 为首屏性能、数据请求、交互响应和前端领域化架构版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增 Web Vitals 与 Long Task RUM 上报入口，持续采集 LCP、INP、CLS 等首屏和交互指标。
- 新增表单启动聚合接口 `GET /api/forms/{formUuid}/bootstrap`，一次返回表单元数据、已发布 Schema、权限过滤后的导航和首屏记录。
- 新增记录查询接口 `POST /api/forms/{formUuid}/records/query`，支持服务端分页、筛选和排序；筛选/排序字段会依据已发布 Schema 校验，数字与日期字段使用安全类型比较。
- 新增前端 `features/` 领域目录，包含 `agent-assistant`、`records`、`form-runtime`、`automation-editor` 和共享类型入口。
- 新增 Agent SSE discriminated union、会话/消息/记录查询 Hook、记录 Mutation、表单运行时字段注册和自动化编辑器领域入口。
- 新增 Excel Worker，负责 Excel 解析、表头字段匹配校验和工作簿生成；ArrayBuffer 使用 transferable 传输。
- 新增导航分组更新与删除接口 `PATCH/DELETE /api/apps/{appId}/navigation/groups/{groupId}`；请求与响应模型均纳入 `ToSchema`、OpenAPI 导出和前端 SDK 生成。
- Agent 新增创建、读取和删除导航分组工具，并可使用真实分组 ID 创建表单或移动表单；删除分组保持确认边界，且不会删除分组内的表单或子分组。
- Agent 新增发布表单与永久删除表单工具，并补入平台“允许工具”目录、默认表单 Skill 和待确认执行链；永久删除始终要求人工确认。
- 新增表单名称更新与导航移动接口；表单改名会生成只修改 `formName` 的新 Schema 草稿版本，导航移动可明确指定目标分组或顶级。

### 性能优化

- 许可证校验不再阻塞应用首屏，改为固定尺寸应用壳与状态遮罩；表单、侧栏、记录表和 Agent 区域使用固定尺寸 Skeleton 降低 CLS。
- 根布局改为 Server Layout；认证、许可证、侧栏和应用壳仅包裹受保护路由，登录页和公开页面不再加载全局客户端边界。
- Agent、Tiptap、XYFlow、Monaco 等重型依赖按路由懒加载，XYFlow CSS 改为路由级加载。
- React Query 统一服务端状态，支持缓存、预取、取消、分页数据保留和失效刷新；Agent 会话与消息切换不再重复加载整个助手。
- SSE 增量消息按动画帧批量更新；Agent 消息超过 200 项启用虚拟滚动。
- 记录表改为服务端分页，浏览器只处理当前页；搜索使用 `useDeferredValue`，并在记录变化时预计算搜索文本，避免输入期间重复序列化完整记录。
- Excel 解析与导出移出主线程；设计器历史快照延迟到 debounce 到期后创建，并使用 `structuredClone`，减少连续编辑时的主线程深拷贝和序列化。

### 架构与维护

- 迁移受保护路由至 `(protected)` 路由组，拆分根客户端边界和应用级布局。
- Agent、记录、表单运行时和自动化编辑器的领域 API、类型、Hook、Mutation 与组件入口集中到 Feature-based 目录；UI 不再直接实现记录/Agent 请求协议。
- 统一 `ApiEnvelope`、`FormRecord`、Agent 消息和 SSE 事件类型；业务组件未新增 `any`。
- API 请求统一支持默认超时、调用方取消、网络错误、HTTP 错误和业务错误分类；生成 SDK 通过 codegen 兼容脚本自动使用受控 fetch。
- 新增 Markdown 与纯 HTML 两种系统架构文档，梳理 Next.js 路由与 BFF、Feature-based 前端领域、Rust/Axum API、Agent 工具执行、PostgreSQL、Valkey、运行时文件、外部服务及单容器部署边界。

### 交互优化

- Agent 工具调用标签新增应用、表单和集成自动化资源名称，便于从会话记录识别每次调用的目标资源。
- Agent 消息输入框改为随内容自动增高，最多显示 13 行；图片上传与发送按钮移至输入框底部工具栏，并补充抽屉底部间距。
- 表单导航分组在拖动按钮左侧新增“更多”菜单，支持修改名称、移动到其他分组或顶级、删除；移动目标会排除自身及后代，避免循环嵌套。
- 表单导航中的普通、流程和自定义表单新增“更多”菜单，支持修改名称、移动到分组或顶级、永久删除；导航分组首次加载时默认折叠。
- 删除导航分组时仅移除分组容器，内部表单和子分组自动上移到原父级，并在确认弹窗中明确提示数据保留规则。

### 修复

- 修复 Next.js 路由迁移后回收站页面缺少 Suspense 边界导致生产构建失败的问题。
- 修复记录查询按文本比较数字和日期导致排序错误的问题。
- 修复 Agent 抽屉关闭后无法再次打开、确认消息需重开抽屉才显示，以及空闲查看历史消息时仍因高频批量更新掉帧的问题。
- 修复 Agent 创建表单无法指定真实导航分组的问题，补齐分组读取、父分组参数和移动表单能力。
- 修复导航分组动态更新与删除接口缺少 Next.js BFF 路由而在前端返回 `404 Not Found` 的问题。

### 验证

- Rust `cargo check` 与 35 项后端测试通过；OpenAPI 导出和前端 SDK codegen 通过。
- TypeScript、ESLint、`git diff --check` 和 Next.js 生产构建通过，78 个页面成功生成。

## 0.97a - 2026-08-03

> `0.97a` 为平台许可证、即时通讯、数据回收与平台运维能力版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增 RS256 平台许可证校验与激活入口。平台许可证必须包含 `platform` 模块；受保护 API 会在请求时向许可中心确认许可证未被吊销，未激活、过期或已吊销时仅保留许可证设置入口。
- 新增由许可证 `communication` 模块控制的即时通讯：支持单聊、群聊、会话未读数、历史消息、已读状态、两分钟内撤回、重新编辑、表情、附件及 WebSocket 实时事件；群主可维护成员、转让群主或解散群聊，成员可主动退出。
- 新增通讯设置、存储统计与清理接口，可配置附件、表情和聊天内邮件消息的使用范围及消息保留期限；通讯模块未授权时不会创建通讯数据表或显示消息入口。
- 新增表单记录回收站。删除记录会保留原始数据、来源表单和删除时间，用户可按表单查看、恢复、永久删除或清空记录，并可设置 1 至 3650 天的保留期限。
- 新增平台日志浏览和清理接口，用于查看运行与异常事件及当前日志占用空间。
- 新增自动化条件分支的可视化规则编排，支持按优先级、规则组或表达式选择分支；数据节点补充字段映射编辑器和上游数据字段选择。
- 新增消息工作台、回收站页面和许可证管理弹窗：侧栏会按许可证与权限显示消息和回收站入口、显示会话未读数，并在平台许可证不可用时引导有权限的用户完成更新。
- 表单运行时新增记录详情辅助面板与表单 Agent 面板；自定义数据视图可配置列显示、排序、拖拽顺序、固定列和列宽。

### 调整

- Agent 模型供应商、配置档案、Agent 定义及其资源关联迁移至数据库持久化；启动时会迁移既有本地 Agent 注册表，继续保留其作为兼容来源。
- OpenAPI 契约改由后端导出到 `api/openapi/openapi.json`，前端代码生成直接使用该文件；开发启动流程同步移除旧的 OpenAPI 合并基线与地区目录静态副本。
- 应用设置拆分为基础、表单、管理员、权限与数据工厂子路由；平台设置补充通知、日志、回收站、许可证与通讯配置入口。
- 表单记录读写、自动化执行和工作流相关查询补充缓存失效与权限边界处理；流程通知偏好可单独读取与配置。
- 前端将表单运行时和自动化编辑器拆分为独立的记录表格、字段控件、动作引擎、条件规则、节点配置及字段映射组件，收敛复杂页面的状态与复用逻辑。
- 工作流通知入口由应用页头调整到用户菜单；模型供应商设置补充常用服务商预设与图标，便于快速创建提供商配置。

### 修复

- 修复条件节点未命中分支时可能静默沿用首条连线的问题，现在会明确将流程实例标记为失败。
- 修复并发处理“任一同意”审批任务时可能重复推进后续节点的问题，通过实例节点状态更新确保只有一个请求创建后续任务。
- 修复删除表单或明细表单后遗留回收站条目的问题，删除定义时会一并清理关联回收站数据。

## 0.82a - 2026-07-27

> `0.82a` 为 Agent 受控执行、四类表单理解、关联数据分析、字段级数据保护及流程审批运行时增强版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- Agent 会话新增所属用户绑定；创建、读取、继续、重命名、置顶、删除及待确认操作均校验当前 JWT 用户，避免跨用户访问会话或代办写入请求。
- Agent 对话新增按运行查看分析依据：助手消息关联本次运行 ID，用户可在会话中按需查看所调用的工具、参数和执行状态；追溯接口复用会话所属用户校验，不返回完整工具输出或业务记录。
- 新增 Agent 执行范围模型，支持平台、应用和业务三个范围；工具调用会校验当前应用上下文、角色可见应用及工具授权，跨应用资源不会因模型指令直接读取或操作。
- 新增上下文资源校验，Agent 引用表单、应用及相关资源时会检查其归属与当前工作范围，避免无效或越界上下文进入模型执行链路。
- 新增 Agent 待确认操作机制。涉及创建或保存的工具先生成持久化待办，再由用户确认或取消；前端会话支持显示待确认卡片，并可在恢复会话后继续处理。
- 新增 `create_form_draft` 受控创建能力，可生成普通表单、流程表单及定义型表单的待确认配置；明细表单维持依附父表子表单的创建语义。
- 新增业务上下文读取工具，可汇总当前应用的说明、表单、字段摘要、关联关系及明细父表信息，供 Agent 在有限且明确的业务范围内理解应用结构。
- 应用设置新增 Agent 业务上下文，管理员可维护业务概述、术语与指标口径、业务流程和分析约束；业务地图工具会在授权应用内将这些配置与表单结构一并提供给 Agent。
- 新增表单记录筛选工具，支持等于、包含、为空三种条件及字段投影，适合在 Agent 分析前逐步缩小记录范围。
- 新增表单记录聚合工具，支持计数、求和、平均值和分组计数，可结合受控筛选条件生成分析结果。
- 新增表单关系图与关联记录追溯工具，可读取关联字段定义，并沿已配置的关联表单字段查询相关记录。
- 新增流程表单只读分析工具，可读取流程定义以及指定记录的流程运行状态、实例和任务信息；Agent 不具备提交、审批、拒绝或反审权限。
- 新增明细表单工具，可读取明细定义、父表关联和明细记录；也可生成明细配置的待确认创建请求，确认后按既有平台语义创建。
- Agent 字段属性新增 `agentDataAccess` 配置，支持“允许读取、脱敏显示、禁止读取”三种数据访问策略；表单设计器属性面板同步提供该配置入口。
- 新增流程任务中心，内置“待我处理、我处理的、我创建的、抄送我的”四类工作台页面；任务可按当前登录用户和应用查询，并可跳转回对应流程记录。
- 新增待办审批操作界面。审批任务支持同意、拒绝及审批意见，执行任务支持完成；拒绝意见必填，操作后自动刷新任务状态。
- 新增流程记录详情轨迹与独立评论。轨迹合并表单变更、提交、同意、拒绝及审批意见；评论使用 `workflow_comments` 独立持久化，不与审批决定混用。
- 新增站内流程通知。审批、执行和抄送任务创建时写入 `workflow_notifications`；导航栏用户头像菜单提供 HeroUI 未读徽章、通知列表、已读标记及关联记录跳转。
- 新增流程实例暂停与恢复。实例保存暂停人、时间和原因；暂停期间任务操作被后端拒绝，恢复后原待办可以继续处理。

### 调整

- Agent 的表单读取能力扩展为列表读取、条件查询、聚合、关系追溯和业务上下文组合调用，使其可在当前应用内连续查询多组记录后汇总分析，而非只读取单条记录。
- 查询与聚合改为有界扫描：单次列表/条件读取受分页约束，聚合最多扫描 10 页、每页 100 条记录，并在结果中保留扫描范围说明，避免无界数据读取。
- 关联记录追溯仅使用表单 Schema 中已配置的 `associationFormField` 关系，并限制源记录数量；关联目标仍受当前应用上下文约束。
- 默认 Agent 配置档案、内置 Skill 授权和平台工具目录同步补齐新增的表单、记录、关系、流程及明细工具，使能力开关与运行时实际可用工具保持一致。
- Agent 的普通、流程、定义型及明细表单操作统一纳入确认链路；Agent 可以准备受控草稿和变更，不会直接发布、删除或绕过既有表单生命周期。
- 定义型表单的 Schema 草稿在 Agent 确认执行时复用公开的表单类型校验，HTML/TSX 组件范围、HTTPS 外部资源和脚本 SRI 完整性要求不会因 Agent 路径绕过。
- 字段输出在进入模型上下文前统一应用访问策略：显式禁止读取的字段会移除，显式要求脱敏的字段会隐藏敏感内容；未配置策略时，凭据、令牌、密码、密钥、身份证及银行卡类字段默认拒绝，手机号和邮箱默认脱敏。
- Agent 读取表单、关联、明细及流程记录时会实际应用角色已配置的表单数据范围：全部、本人、本部门、下级部门或无数据权限；多角色同时生效时按更严格范围处理，身份无法唯一映射的历史记录在受限范围内默认不返回。
- 明细记录读取补齐字段级 `agentDataAccess` 处理，并将明细父记录、关联源记录、关联目标记录和流程运行时记录纳入相同的数据范围校验；受限范围内不会返回全局记录总数。
- 条件查询、字段投影、关联目标字段读取及聚合的筛选、值字段、分组字段均会拒绝使用标记为禁止读取的字段，防止通过筛选或统计间接暴露受保护数据。
- 流程任务的受理人匹配同时兼容稳定用户 ID 与历史显示名称；任务、通知和评论均保留表单、记录及流程实例关联，供前端工作台和详情页复用。
- 多人审批的“任一同意”模式改为首位审批人同意后立即推进，并将同节点其余待办标记为 `skipped`；“全部同意”继续等待全部审批人完成。
- 流程运行时为节点推进增加实例节点条件更新，多个审批人同时处理“任一同意”时仅一个请求可创建后续任务；流程定义只有 `enabled` 状态允许新记录提交，存量实例不受定义停用影响。
- 条件节点按分支优先级、规则或表达式选择对应连线；未命中任何分支会将实例标记为失败，不再静默走第一条边。
- 流程评论和运行轨迹读取显式校验表单与所属应用的显示权限；暂停操作补充可选原因输入并保存至实例操作历史。

### 修复

- 修复 Agent 工具调用只依赖工具授权而未完整绑定会话所属用户和当前应用范围的问题，补齐会话、资源和 RBAC 的联合校验。
- 修复 Agent 保存定义型表单 Schema 时未经过与常规设计器一致的类型安全校验的问题，避免自定义页面组件或外部脚本约束被确认路径绕开。
- 修复流程任务查询中 SeaORM 子查询 API 使用不匹配的问题，恢复工作流运行时相关查询的编译与执行兼容性。
- 修复流程表单页仅具备提交/反审入口、但缺少实际待办处理界面的断点，并补齐 Next BFF 对流程运行态、任务动作、评论、通知、暂停和恢复接口的代理。

## 0.78a - 2026-07-24

> `0.78a` 为定义型表单、子表明细、Agent 协助和表单设计体验增强版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增定义型表单，可在表单设计器中使用 HTML 和 TSX 自定义组件；组件在受限 iframe 中运行，只能读取表单上下文并向获授权字段回写值。
- 定义型表单支持登记 HTTPS 页面资源；脚本资源要求配置 SRI 完整性校验，后端会校验资源来源、数量、标识和代码大小。
- 新增子表明细表单：可从已发布的子表单生成独立的明细表单和导航入口，以单行表单方式查看、创建、编辑和删除子表记录，并配置主、副展示字段。
- 流水号组件新增可排序的规则编排，支持自动计数、固定字符、提交日期和表单字段片段，并可设置补零、初始值和按日、月、年重置。
- 新增平台级 Agent 协助设置，可指定导航助手使用的 Agent，并维护表单 Schema 分析的全局提示词。
- Skills 管理支持导入 ZIP 格式的 Skill 包；平台工具目录补充应用列表读取和表单草稿保存能力，供 Agent 配置档案按授权范围使用。

### 调整

- Agent 助手会话新增来源分类、置顶、重命名、删除和按时间分组浏览；会话列表支持按通用对话、Schema 分析和表单填写筛选。
- 默认数据视图改为可持久化保存配置且不可删除，确保表单始终保留“全部数据”入口。
- 表单设计器补充定义型组件代码、外部资源来源、附件大小限制和地区级联深度等属性配置，并优化组件属性面板、预览和运行时布局。
- 流程表单运行时、子表数据读写、表单导航和记录详情同步适配明细表单及扩展后的字段类型。
- OpenAPI 合并逻辑改为合并同一路径下的操作，避免基础契约覆盖生成契约中的其他 HTTP 方法；前端 SDK、类型定义和开发启动脚本同步更新。
- 设置中心统一使用页面标题、说明、操作区和固定底部保存区的布局；模型供应商、Agent、配置档案、插件、Skill 与知识库等资源页同步收敛交互和状态反馈。
- 用户管理改为可组合的来源、状态筛选和信息表格，提供集中操作菜单；角色管理改为卡片视图；组织架构补充组织统计、节点详情及直属成员查看。
- 权限设置改为先选择角色、再按应用和平台功能分组授予权限，支持单项勾选与一键授予分组全部权限；系统管理员权限仍受保护不可修改。
- 身份源设置明确区分本地、钉钉及其他预留身份源的配置状态，并将保存操作收敛到页面底部，减少配置过程中的误操作。

### 修复

- 修复开发后端已运行但二进制落后于源代码时未自动重启的问题，启动脚本现在会检测并重启过期的 `yaya-api` 进程。
- 修复 Agent 会话排序和持久化缺少来源、置顶状态的问题，并补齐现有数据库的兼容初始化与索引。
- 修复删除详情表单时错误沿用普通表单动态存储清理流程的问题；详情表单现在仅清理其定义、Schema 与导航数据。

## 0.67a - 2026-07-23

> `0.67a` 为表单富文本、级联地区目录和运行时体验完善版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增富文本组件，支持段落、标题、文本样式、颜色、对齐、列表、引用、链接、图片、表格和任务列表，并在表单运行时展示只读内容。
- 新增级联选择组件及数据源编辑器，支持多层级选项、中文/英文标签和路径值保存；地区字段升级为通用地区目录树，支持国家、地区、城市及更多层级的目录导入、查询和多语言显示。
- 新增地区目录 API 与命令行导入能力，可通过 `--import-location-catalog <目录>` 导入已校验的 `locations.json` 树形目录。
- 新增通用文件上传下载 API，文件按日期分目录存储，并限制单个文件最大 20 MB。
- 新增关联表单字段在记录表格中的主字段展示与详情弹窗，可直接查看关联记录的只读表单内容。
- 数据库设置新增连接状态、错误详情和保存前的独立连接测试；数据库密码允许为空，以支持无密码 PostgreSQL 实例。

### 调整

- 表单设计器完善富文本、级联选择和流水号的布局与校验约束：富文本自动占满可用宽度且不能加入子表单；主画布及每个子表单各限一个流水号；分组容器会随新增或移动组件自动扩展。
- 自动化新建流程改为分别选择触发时机和操作类型，并允许编辑自动化名称；流程表单缺失绑定的流程自动化时，可从设计器自动恢复创建。
- 保存表单草稿时会识别无有效 Schema 变更，避免生成多余的版本记录。
- 表单运行时补齐级联、地区、富文本和关联字段的显示、Agent 填写校验及记录列表格式化；OpenAPI 契约、前端 SDK 与类型定义同步更新。

### 修复

- 修复设计器拖拽、缩放和嵌套容器放置时的布局计算问题，避免重复缩放累积和无效的自放置操作。
- 修复地区目录从旧版 `parent_code` 结构升级到树形结构时的兼容与恢复流程，并为旧记录提供读取时转换。
- 修复流程表单绑定的流程自动化被缺失或删除后无法从设计器进入流程配置的问题。

## 0.62a - 2026-07-22

> `0.62a` 为流程表单与人工审批工作流版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增流程表单类型。创建流程表单时会自动生成与该表单一一对应的审批流程，并在应用导航、表单列表和应用设置中以独立图标及类型标识展示。
- 新增流程工作流运行时：支持提交已保存记录、发起流程实例、生成待办任务、同意、拒绝和已审批记录反审；实例、任务和操作历史均持久化，并保留所用流程版本。
- 流程编辑器新增审批、执行、抄送和结束节点；审批/执行节点可配置处理人及会签方式，流程可复用数据操作和 HTTP 请求节点。
- 流程表单记录新增审批状态、实例状态、当前审批节点和提交人等可配置表格字段；记录页提供提交与反审操作。
- 新增通用工作流画布、节点注册表、属性抽屉和图校验基础组件，覆盖起始节点、断链、循环、孤立节点及节点配置校验。
- 新增应用导航项排序接口，以及表单创建类型、应用所有者头像、记录创建者头像和所属组织等接口字段。

### 调整

- 自动化定义区分事件触发流与流程流：事件触发执行不会误触发流程流，流程流固定绑定其流程表单及 `form_submit` 触发事件，并限制可用节点类型。
- 应用创建者改为当前登录用户，应用列表与卡片可展示所有者头像；表单记录列表补充创建者身份和组织信息。
- 表单运行时拆分关联表单、成员选择和子表单表格组件，关联记录与成员目录改为缓存加载；子表单支持按配置分页显示。
- 重构表单视图管理、应用资源加载与自动化编辑器的共享逻辑，收敛重复请求和状态处理。
- OpenAPI 契约、前端 SDK 及类型定义同步更新，以覆盖流程表单、导航排序和补充的资源元数据。

### 修复

- 修复流程任务仅按显示名称校验处理人的问题，现优先按用户 ID 校验，并为待办查询建立对应索引。
- 修复流程定义编辑可能修改绑定表单或触发事件的问题，并阻止普通自动化创建流程提交触发器。

## 0.56a - 2026-07-21

> `0.56a` 为 Agent 可控执行、资源运行时与应用工作区权限细化版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- Agent 配置档案新增创建表单草稿与保存 Schema 草稿的受控能力；Agent 可在获授权的当前应用范围内创建表单，但仍不能发布、删除或修改既有资源。
- Agent 配置档案新增创建应用、创建表单、创建自动化 3 个能力开关；当前运行时已实际接通创建表单与保存 Schema 草稿，创建应用和自动化开关暂未绑定对应工具。
- Agent 运行时接入已绑定的 Skill 指令、知识库内容检索和插件工具清单，并记录解析后的资源审计信息。
- Skill 支持以独立 `.yaya-skills/<package>/SKILL.md` 文件包读写；知识库支持维护正文内容并按用户问题检索相关片段。
- 默认 Agent 配置自动补齐表单设计、表单草稿填写、自动化审查 3 个内置 Skill，以及表单组件与自动化审查 2 个知识库；其中表单设计顾问预授权应用列表、表单列表、表单 Schema、创建表单草稿、保存 Schema 草稿 5 个平台工具；同时提供默认禁用的 HTTP JSON 插件模板。
- 插件支持 JSON Manifest 声明工具，并可在无人工确认要求时受控调用 HTTP 工具端点；新增 HTTPS、私有地址、重定向和响应大小等安全限制。
- 新增平台工具目录、Skill 文件读写、授权集合及应用默认导航入口接口。
- 新增关联表单字段：设计器可选择关联范围、显示字段、筛选条件与回填映射；运行时可读取关联表单记录，选中记录后自动回填目标字段。
- 新增成员选择字段的设计与运行时配置，支持按本地、钉钉、企业微信或飞书来源，以及全部成员、指定角色、指定成员范围进行单选或多选。
- Agent 可在未提交的表单运行时页面协助填写字段值；该模式仅回写浏览器草稿，明确禁止代替用户提交数据。
- 新增表单设计器对关联表单配置的发布前校验，以及关联表单字段的列宽调整能力。
- 自动化字段映射新增可搜索字段和函数的公式编辑器，支持插入常用数学、逻辑与文本函数。
- 应用设置支持选择默认导航入口；新建表单会在尚无默认入口时自动成为默认入口。

### 调整

- RBAC 扩展为平台菜单分组授权及应用内细粒度权限；应用开发明确拆分为编辑应用信息、创建分组、集成自动化、应用设置、应用发布 5 项，并补充表单开发和视图开发入口；前端导航会按授权结果隐藏不可访问项。
- 权限检查从设置页延伸到应用顶部导航、应用侧栏、表单设计器、表单操作和 API 代理；无表单开发权限时，设计器提供明确的受限状态。
- Agent 助手、资源配置档案与平台设置界面补充能力开关、资源内容、插件 Manifest 和平台工具展示，并优化会话与页面上下文传递。
- Agent 助手由居中弹窗调整为右侧抽屉，增加工具调用进行中/完成状态、按最新会话排序、智能自动滚动和回到最新消息操作，并按 Agent 配置权限控制配置入口。
- 自动化编辑器改用抽屉式属性面板，新增 Schema 查看器并收紧映射控件布局；运行时公式与表单设计器的编辑反馈同步优化。
- 组织架构接口和页面改为返回、展示、筛选部门直属成员；角色页和权限页增加身份来源筛选与更清晰的授权状态展示。
- 设计器完善关联表单、成员、子表单和容器的属性配置与布局约束，允许子表单在分组容器中按容器宽度布局，并保持子表内部字段的布局限制。
- 表单运行时补充关联记录选择、成员候选加载、子表行关联字段回填等交互；应用设置、表单工作区、首页、侧栏和设置中心同步调整布局与状态反馈。
- OpenAPI 文档、前端生成 SDK、类型定义和后端路由同步扩展，供前端代理调用新增的授权、平台工具、Skill 文件及默认入口接口。

### 修复

- 修复本地凭据实体、迁移、OpenAPI 契约和生成客户端在新增配置字段后的同步问题。
- 修复应用导航默认入口切换、表单运行时异步加载、钉钉组织同步与多个 API 请求的授权和错误处理边界。
- 修复组件布局计算对未知字段类型的处理，并避免关联表单未完成显示配置时发布无效 Schema。

## 0.5a - 2026-07-20

> `0.5a` 为身份认证、RBAC 和表单数据工作区迭代版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增本地账号密码登录、JWT 会话与钉钉 OAuth 扫码登录；钉钉账号可按配置在首次登录时自动创建平台用户。
- 新增本地用户与本地角色的创建、编辑、删除和角色分配能力，支持维护用户资料、多个邮箱地址与账号状态。
- 新增 RBAC 权限中心，可为角色配置平台设置、应用和表单的显示、编辑、创建、删除等授权；系统管理员保留全量权限。
- 新增认证与授权中间件，覆盖应用、表单、自动化和平台设置接口，并按授权结果过滤应用导航。
- 新增表单自定义视图的服务端持久化及 CRUD，表单记录接口支持分页查询。
- 新增字段大纲页面与接口，可按应用浏览表单、字段、Schema 版本及动态物理表信息。
- 新增后端 `/openapi.json` 契约导出与前后端联合启动脚本，支持在生成客户端前合并 OpenAPI 基线契约。

### 调整

- 身份源设置精简为钉钉配置，补充清理同步数据、AccessToken 管理和 OAuth 回调链路。
- 登录页支持记住密码、自动登录和钉钉授权跳转；侧边栏改为账户菜单，并重构首页、应用中心和设计器入口布局。
- 首页改为连接真实应用数据的工作台，展示最近创建、最近访问、待处理入口及应用统计；最近访问记录保存在浏览器本地。
- 应用中心统一使用页面头部与卡片布局，应用内侧栏、主题切换菜单和账户菜单同步收紧尺寸与交互层级。
- 设置端重构用户、角色、组织架构、身份源与权限页面，补齐本地用户资料、邮箱、角色分配和授权配置的管理交互。
- 表单运行时的视图配置从浏览器本地存储迁移至后端，记录表格补充分页加载、视图切换、列宽计算和请求失败状态处理。
- 设计器画布替换通用字段预览为轻量化预览，优化布局、字段描述、组件箱和工作台面板的显示与编辑反馈。
- 前端 API 代理统一转发浏览器会话令牌，并逐步切换到基于生成客户端的后端 SDK 代理，减少各路由重复的转发逻辑。
- 表单删除流程改为事务化清理关联记录、视图、导航、自动化定义、版本与运行日志。

### 性能优化

- 表单记录查询支持数据库级分页与总数统计，避免数据量增大时一次性加载全部记录。
- 设计器画布与工作台侧栏引入 `memo` 和数据缓存：预计算字段占用单元格，减少拖拽、缩放和属性编辑时的重复扫描与无关面板重渲染。
- 设计器直接操作期间跳过布局动画，并临时关闭画布与记录详情背景的毛玻璃滤镜，降低频繁重绘与合成开销。
- 表单运行时预先构建分组子字段索引，并通过稳定回调和上下文引用降低字段输入时的组件更新范围。
- 公式执行缓存编译结果、字段依赖关系和循环引用分析，避免每次值变更重复解析同一公式。

### 修复

- 修复部分表单与应用侧边栏请求失败时的状态处理，授权不足时不再保留不可访问的导航项。
- 修复表单删除后遗留自动化关联数据的问题。
- 修复运行时公式字段在依赖更新后的计算与错误反馈链路，并改进表格文本宽度估算以减少列宽跳动。
- 修复多处异步请求缺少异常处理、受控状态同步和 React Effect 触发的更新警告。
- 将 OpenAPI 生成文件加入忽略规则，避免自动生成产物进入版本变更记录。

## 0.3b - 2026-07-20

> `0.3b` 为前后端工程拆分与桌面端支持版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增 Tauri 2 桌面客户端壳层，支持在桌面窗口中连接本地或独立部署的 Next.js Web 服务。
- 新增仓库级开发、构建和检查命令，统一管理 Web、API 与桌面端工作流。
- 新增 Tauri 桌面应用图标、权限配置及 Windows 可执行文件构建能力。

### 调整

- 将 Next.js 前端项目迁移至 `web/`，包名调整为 `@yaya-low-code/web`。
- 将 Rust 后端项目从 `backend/` 迁移至 `api/`，Cargo 包名调整为 `yaya-api`。
- pnpm workspace 改为以仓库根目录管理 `web` 项目，API 继续保持独立 Cargo 工程。
- 更新 README、构建脚本、忽略规则和 Turbopack workspace root 配置以适配新目录结构。

### 修复

- 修复表单视图、自动化列表、成员选择器、运行时表单和 Agent 设置页面中的 React Hook Effect 同步状态更新问题。
- 修复表单视图 ID 生成触发的 React 纯度检查错误。
- 清理未使用的表单记录过滤变量、无效 ARIA 属性和 Tauri 构建目录的 ESLint 扫描警告。

## 0.3a - 2026-07-15

> `0.3a` 为 Alpha 阶段的存储模型与工作区体验迭代版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增按表单编译的动态混合存储：已发布表单拥有独立物理数据表，标量字段映射为带类型的数据库列，复杂字段保留在扩展 JSONB 中。
- 新增子表单子表与同步机制，支持子表单行数据独立落库，并在表单 Schema 更新时回填对应数据。
- 新增 `form_storage_definitions` 存储定义表，保存表单物理表、字段映射和已编译 Schema 版本。
- 表单设计器新增字段索引配置面板，支持为可映射的标量字段配置 B-tree 索引。
- Agent 注册配置补充插件、Skill、知识库定义 CRUD，并支持在 Agent 配置档案中绑定这些资源。

### 调整

- 表单记录仓储改为存储无关接口，表单运行时和自动化节点均通过动态表读写记录。
- 表单创建、发布和 Schema 更新流程会同步存储计划；表单与应用删除时会清理对应的动态表和存储定义。
- 表单详情工作区重新组织工具栏：筛选、显示列、排序靠近表单编辑；记录操作使用带提示的图标按钮；搜索改用 HeroUI `SearchField`。
- 更新应用侧栏、内容区和表单导航布局，并新增卡片毛玻璃语义色；亮暗主题背景渐变与卡片表面调整为更易区分的层次。

### 修复

- 移除运行时共享 `form_records` 表及其实体，避免所有表单记录继续堆积在共享 JSONB 结构中。
- 存储计划编译时拒绝重复字段 ID，避免字段列映射冲突。
- 修复表单 Schema 更新后子表单投影未回填的问题。
- 表格区域改为按需显示纵向滚动条，移除右侧始终预留滚动条造成的背景缝隙；操作列会占用剩余可用宽度。

### 当前限制

- 动态表建表、索引和子表单触发器尚缺少真实 PostgreSQL 的端到端集成测试。
- JSONB 仍有意用于复杂字段扩展数据、子表单行数据和存储计划元数据，不再作为共享表单记录的唯一持久化模型。
- 插件、Skill 和知识库当前完成配置管理与绑定，运行时插件加载、Skill 执行和向量检索仍待实现。

## 0.28a - 2026-08-04

> `0.28a` 为中期迭代版本；npm 与 Cargo 包版本仍标识为 `0.2.0-alpha.0`。

### 新增

- 新增 Agent 多级管理体系：
  - 模型提供商（Provider）配置与 CRUD 页面 `/settings/model-providers`
  - Agent 配置档案（Config Profile）与 CRUD 页面 `/settings/agent-profiles`
  - Agent 定义（Agent Definition）管理页面 `/settings/agents`
  - Agent 人格（Persona）预设（默认人格、业务分析师、低代码实施顾问）
  - 插件（Plugin）、技能（Skill）、知识库（Knowledge Base）定义与管理 API
- 新增 Agent 注册表（Agent Registry）后端持久化，支持按平台/应用/业务范围解析 Agent 配置，兼容旧版 Agent 设置自动迁移。
- 新增身份源管理：
  - 钉钉集成：AccessToken 获取与刷新、部门同步、用户同步的后端接口
  - 平台用户体系，支持本地和钉钉双身份来源
  - 身份源设置页面 `/settings/identity-source`，配置保存后立即生效
- 新增身份与权限数据模型与数据库迁移：
  - `organization_units` 组织单位表
  - `iam_users` / `iam_external_identities` 用户与外部身份表
  - `iam_roles` / `iam_user_roles` 角色与用户角色关联表
  - `iam_organization_memberships` 用户组织成员关系表
  - Agent session 绑定 agent_id 迁移
- 新增子表单（Subform）组件运行时渲染：
  - 表格视图，支持行新增、删除、复制、上移/下移排序
  - 斑马纹、分割线、边框三种主题
  - 序号列、操作列显示控制，自定义列宽与冻结
  - 批量导入、导出 Excel、批量删除、子表单标题过滤
  - 布局模式（auto/fixed）、每页行数、最大行数限制
- 新增表单运行时 `onValuesChange` 回调和 `valuePatch` 外部值同步能力。
- 新增 Agent Markdown 渲染组件（`react-markdown` + `remark-gfm`）。
- 新增 `xlsx` 依赖用于 Excel 批量导入导出。
- 新增表单 GET API 端点（`GET /api/forms/:form_uuid`）。
- 新增设计器校验模块（`designer-validation.ts`）。
- 新增设置页面子路由：模型供应商、Agent 管理、配置档案、身份源、组织架构、插件、Skills、用户、角色。

### 调整

- Agent 设置页面 `/settings/agent` 改为自动重定向到 `/settings/agents`。
- 设置导航从 4 项扩展为 13 项，支持多级分组与独立路由。
- 设计器工作台、画布、侧边栏、字段属性面板、页面属性面板、布局系统大幅重构，增强交互体验和视觉一致性。
- 设计器新增调试事件通道、页面级 Agent 上下文配置等能力。
- 全局 CSS 主题变量、玻璃拟态样式、布局高度和滚动容器全面优化。
- 运行时表单分组容器边距缩减（`p-4` → `p-1`），Radio/Checkbox 选项文本字号调整为 12px。
- Agent 助手面板重构，支持模型切换、上下文统计和更丰富的会话交互。
- 后端设置模块扩展：新增身份源配置读写、Agent 注册表读写、Agent 配置按范围解析。

### 修复

- 修复表单预览和运行时下拉单选/下拉复选组件（SelectPreview / MultiSelectPreview）从非受控改为受控模式，解决选择其他选项后显示不更新的问题。
- 修复后端 Agent 流式响应中 `assistant_message` 插入数据库的 `Result` 未处理问题，添加 `error!` 日志和 `message.persist_failed` SSE 事件通知。
- 移除运行时表单页面对本地 `testSchema.json` 的硬编码依赖，改为完全从 API 动态加载 Schema，并添加 schema 为 null 时的加载状态保护。
- 新增 `.yaya-agent-registry.json` 和 `.yaya-identity-settings.json` 到 `.gitignore`，防止敏感配置被误提交。

### 当前限制

- 知识库与 Skills 后端定义已完成，前端页面仍为功能占位，尚未接入向量检索和 Skill 加载器。
- 插件机制目前为管理端的定义配置，尚未实现运行时插件的加载与执行。
- 子表单当前仅支持运行时渲染，设计器端拖拽和属性配置尚未完成。
- 身份源的用户/角色列表当前为只读查询，尚未接入权限校验中间件。
- 项目仍处于 Alpha 阶段，接口、数据结构与交互可能继续调整。

## 0.2a - 2026-07-13

> `0.2a` 为早期预览版本；npm 与 Cargo 包版本使用语义化标识 `0.2.0-alpha.0`。

### 新增

- 新增平台级 Agent MVP：
  - 支持 OpenAI Compatible 模型供应商、API Base URL、对话模型、Embedding 模型、Temperature 与最大执行步骤配置。
  - 新增全局 Agent 助手入口、会话列表、历史消息与 SSE 流式输出。
  - 新增只读工具，可读取应用表单列表、表单草稿 Schema、自动化列表与自动化流程图。
  - 新增 Agent 会话、消息、运行与步骤审计表：`agent_sessions`、`agent_messages`、`agent_runs`、`agent_run_steps`。
- 新增平台设置子路由与左侧导航：
  - `/settings/database`
  - `/settings/agent`
  - `/settings/knowledge`
  - `/settings/skills`
- 新增应用设置页面，提供基础设置、表单设置、应用管理员、权限和数据工厂等配置入口；当前除应用名称加载外，其余配置为前端占位。
- 新增系统、浅色、深色三种主题模式与主题切换菜单。
- 新增表单默认值公式能力、公式函数定义与运行时公式求值支持。
- 新增应用详情代理接口，支持应用设置页读取当前应用名称和基础信息。
- 新增基于 `@dnd-kit/core` 的导航与设计器拖拽交互支持。

### 调整

- 重构全局页面外壳、侧边栏、首页、应用列表与应用内导航，统一玻璃拟态主题变量和响应式布局。
- 重构表单设计器工作台、组件箱、画布、预览、字段属性与页面属性面板，增强历史版本读取和表单名称编辑体验。
- 优化自动化列表与编辑器布局，增强节点配置、条件分支、字段映射、表达式引用和运行日志展示。
- 设置页面改为固定占用主区域剩余高度；左侧导航和右侧设置面板分别独立滚动。
- 数据库和 Agent 配置改为后端本地持久化，敏感字段不回显到浏览器。
- 后端增加 Agent 数据库迁移、实体、路由、模型执行器与只读工具模块。

### 修复

- 修复设置内容较多时面板超出屏幕、内容较少时未撑满剩余高度的问题。
- 修复 Agent 启动器 Hook 依赖与本地消息 ID 生成导致的 ESLint 错误。
- 移除 README 和后端默认连接字符串中的明文数据库密码。
- 将数据库与 Agent 本地设置文件加入 Git 忽略规则，避免 API Key 或数据库凭据被误提交。
- 修复多处布局高度、滚动容器、主题颜色、表单预览和自动化编辑交互问题。

### 当前限制

- Agent 当前仅允许调用只读工具，不会创建、修改、发布或删除业务数据。
- 知识库与 Skills 页面当前为功能占位，尚未接入向量检索和 Skill 加载器。
- 项目仍处于 Alpha 阶段，接口、数据结构与交互可能继续调整。

## 2026-06-29

### 新增

- 新增集成自动化模块，支持应用级自动化列表、自动化编辑页与工作流保存。
- 新增自动化触发器节点、查询节点、新增数据节点、更新数据节点、删除数据节点、HTTP 请求节点、条件分支节点。
- 新增自动化版本快照能力，支持版本列表查询与历史版本恢复。
- 新增自动化结构化持久化表：
  - `automation_flows`
  - `automation_flow_versions`
  - `automation_nodes`
  - `automation_edges`
- 新增自动化运行日志能力，支持查看流程运行记录、节点执行记录、输入输出载荷、耗时与重试来源。
- 新增失败处理能力：
  - 整个流程重头触发
  - 错误节点断点重试
- 新增表单记录运行时编辑、删除接口，并接入自动化触发链路。
- 新增前端 Next.js 代理接口，用于转发自动化详情、版本、运行日志与重试相关请求。

### 调整

- 将自动化编辑器重构为更适合复杂节点配置的暗色工作区布局，属性面板仅在选中节点时显示，最大宽度限制为 `500px`。
- 将工作流名称、说明、启停状态与版本管理上移到编辑器左上角头部区域，移除触发器节点中的工作流级配置项。
- 调整自动化画布交互，点击空白区域时隐藏属性配置，减少右侧配置区常驻造成的干扰。
- 调整条件分支节点的数据源策略，改为可使用全部上游节点产生的数据，而不是仅使用直接上级节点。
- 调整条件分支配置模型，支持：
  - 所有数据均可通过
  - 按条件规则进入
  - 按公式定义进入
- 调整触发器节点配置逻辑：
  - 初始节点名称、说明不可编辑
  - 变更字段仅在编辑前/编辑后事件下展示
- 调整应用内导航，在集成自动化页面隐藏不必要的表单导航干扰项。
- 更新 OpenAPI 描述与前端生成客户端，补齐本轮自动化相关接口的类型定义。

### 修复

- 修复非触发器节点错误显示工作流配置的问题。
- 修复节点属性抽屉在选择部分配置值后意外关闭的问题。
- 修复拖拽连线到空白画布后未弹出新增节点对话框的问题。
- 修复自动化编辑器布局撑高页面、导致浏览器出现多余滚动条的问题。
- 修复运行日志接口在前后端版本不一致时返回空响应，前端表现为 `503` 的问题；现在会返回明确的重启后端提示。
- 修复自动化运行日志查看路径 `404` 排查链路，确认问题来源为旧版后端进程未重启。
- 修复新增字段配置中公式输入框、多条数据来源节点映射等错误交互。
- 修复触发器节点以外的节点仍可编辑不该暴露字段的问题。

### 说明

- 当前自动化运行已支持触发事件：
  - `before_create`
  - `after_create`
  - `before_update`
  - `after_update`
  - `before_delete`
  - `after_delete`
- 当前自动化执行已支持节点类型：
  - 获取单条数据
  - 获取多条数据
  - 新增数据
  - 更新数据
  - 删除数据
  - HTTP 请求
- 当前错误节点重试已经从“整条流程重新执行”调整为“基于失败节点输入快照的断点恢复执行”。
- 若本地仍访问不到运行日志接口，需要确保后端已在本轮代码更新后重新启动。

### 后续优化

- 为自动化运行日志增加后端分页、筛选、统计汇总与日志保留策略。
- 为 HTTP 请求节点补充更完整的响应状态、响应头与错误上下文展示。
- 统一自动化接口到前端生成 API Client，减少运行日志相关页面对手写 `fetch` 的依赖。
- 为自动化执行链路增加事件来源标记、递归深度限制与循环触发防护，避免未来出现自动化自触发套娃风险。
- 补齐更完整的条件表达式编辑器、字段映射编辑器与运行前校验能力。

### 文档

- 删除重复职责的 `README.log`，统一由 `README.md` 承载项目说明，由 `CHANGELOG.md` 承载版本更新记录。
- 更新 `README.md`，补充集成自动化、运行日志、自动化存储结构、本地运行说明与下一步规划。

## 2026-06-26

### 新增

- 新增基于 Rust、Axum、SeaORM 的后端服务。
- 新增 PostgreSQL 持久化，用于存储应用、表单、导航、Schema 与表单记录。
- 新增应用管理接口与前端应用列表页面。
- 新增空白应用创建能力。
- 新增应用卡片工具栏能力，包括访问、启用/关闭、编辑名称、设置入口、删除。
- 新增应用内置系统页面导航。
- 新增应用下表单导航与分组导航。
- 新增导航分组递归结构支持。
- 新增导航拖拽排序能力。
- 新增表单设计器页面。
- 新增组件箱、大纲树、数据源面板、动作面板、页面源码面板。
- 新增分组容器组件，支持收纳子组件。
- 新增 Monaco Editor 作为动作脚本编辑器。
- 新增统一动作脚本模型，支持：
  - `didMount(ctx)`
  - `onFieldEvent(ctx)`
  - `onSubmit(ctx)`
- 新增表单预览能力。
- 新增表单 Schema 草稿保存能力。
- 新增表单发布能力。
- 新增表单历史版本查询与恢复能力。
- 新增表单运行时页面。
- 新增表单记录保存与查询能力。
- 新增基于 `@hey-api/openapi-ts` 的前端 API Client 自动生成能力。

### 调整

- 将设计器左侧工作台改为图标收纳式导航。
- 将设计器工作台与设计区域合并为统一工作界面。
- 为设计器左侧工作台增加宽度拖拽调整能力。
- 将动作面板调整为统一脚本编辑方式，减少多入口配置成本。
- 在预览、保存、发布前自动同步动作编辑器内容到 Schema。
- 将页面源码查看改为 Monaco 编辑器只读模式。
- 优化设计器头部布局、版本恢复交互与按钮样式。
- 优化组件箱、数据源卡片与动作上下文展示方式。
- 优化 MyApp 与应用内多处按钮和交互，优先切换为 HeroUI 组件。

### 修复

- 修复多个页面中 `<button>` 嵌套 `<button>` 导致的 hydration 警告。
- 修复部分页面因服务端与客户端结构不一致导致的 hydration 失败问题。
- 修复表单导航排序错乱问题。
- 修复导航拖拽时部分位置不触发接口的问题。
- 修复导航拖拽后残留样式问题。
- 修复分组与表单在导航中的缩进和布局异常问题。
- 修复表单无法正确拖入某些顶级分组的问题。
- 修复表单详情页中编辑、删除、视图切换等交互位置不合理的问题。
- 修复预览模式下重复渲染、重复挂载带来的报错问题。
- 修复 `Maximum update depth exceeded` 相关循环更新问题。
- 修复调试日志列表 key 重复问题。
- 修复预览和运行时包装层使用 `JSON.stringify(...)` 作为 key 导致的不必要重建问题。
- 修复设计器部分面板内容不显示的问题。
- 修复本地开发中部分跳转路径错误的问题。

### 说明

- 设计器预览使用草稿 Schema。
- 运行时页面默认使用已发布 Schema。
- 当前仓库已完成本地 Git 初始化与首个提交。
