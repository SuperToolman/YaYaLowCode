import { agentRuntimeBaseUrl } from "../../../../../_lib/agent-runtime-proxy";
type Context = { params: Promise<{ sessionId: string; fileId: string }> };
export async function GET(request: Request, { params }: Context) { const { sessionId, fileId } = await params; const headers: Record<string,string> = {}; const auth = request.headers.get("authorization"); if (auth) headers.authorization = auth; const cookie = request.headers.get("cookie"); if (cookie) headers.cookie = cookie; const response = await fetch(`${agentRuntimeBaseUrl}/api/agent/sessions/${encodeURIComponent(sessionId)}/files/${encodeURIComponent(fileId)}`, { headers, cache: "no-store" }); return new Response(response.body, { status: response.status, headers: { "content-type": response.headers.get("content-type") ?? "application/octet-stream", "content-disposition": response.headers.get("content-disposition") ?? "attachment" } }); }
export async function DELETE(request: Request, { params }: Context) { const { sessionId, fileId } = await params; return fetch(`${agentRuntimeBaseUrl}/api/agent/sessions/${encodeURIComponent(sessionId)}/files/${encodeURIComponent(fileId)}`, { method: "DELETE", headers: { authorization: request.headers.get("authorization") ?? "", cookie: request.headers.get("cookie") ?? "" } }); }
