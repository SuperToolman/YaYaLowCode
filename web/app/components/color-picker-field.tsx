"use client";
import { Shuffle } from "@gravity-ui/icons";
import { Button, ColorArea, ColorPicker, ColorSlider, ColorSwatch, ColorSwatchPicker, Label } from "@heroui/react";
export default function ColorPickerField({ label, value, swatches, onChange }: { label: string; value: string; swatches: string[]; onChange: (value: string) => void }) { return <ColorPicker aria-label={label} value={value} onChange={(color) => onChange(color.toString("hex"))}><ColorPicker.Trigger aria-label={label}><ColorSwatch size="lg" /><span className="min-w-0 flex-1"><Label className="block text-sm font-medium text-foreground">{label}</Label><span className="mt-0.5 block text-xs text-muted">Selected: {value.toUpperCase()}</span></span></ColorPicker.Trigger><ColorPicker.Popover className="w-64"><ColorArea aria-label={`${label}颜色区域`} colorSpace="hsb" xChannel="saturation" yChannel="brightness"><ColorArea.Thumb /></ColorArea><div className="flex items-center gap-2"><ColorSlider aria-label={`${label}色相`} className="min-w-0 flex-1" channel="hue" colorSpace="hsb"><ColorSlider.Track><ColorSlider.Thumb /></ColorSlider.Track></ColorSlider><Button isIconOnly size="sm" aria-label="生成随机颜色" onPress={() => onChange(`#${Math.floor(Math.random() * 0xffffff).toString(16).padStart(6, "0")}`)}><Shuffle /></Button></div><ColorSwatchPicker aria-label={`${label}色板`} size="xs">{swatches.map((color) => <ColorSwatchPicker.Item key={color} color={color}><ColorSwatchPicker.Swatch /></ColorSwatchPicker.Item>)}</ColorSwatchPicker></ColorPicker.Popover></ColorPicker>; }
