"use client";

import { useEffect, useState } from "react";
import { useSearchParams } from "next/navigation";
import { Button, Drawer, Modal, Table, toast } from "@heroui/react";
import { ArrowRotateLeft, Eye, TrashBin } from "@gravity-ui/icons";
import { RuntimeFormRenderer, type RuntimeFormSchema } from "../components/runtime-form-renderer";
import { getFormSchema } from "../lib/api-client";

type Entry = { id: string; formUuid: string; recordUuid: string; sourceFormName: string; formType: string; deletedAt: string; expiresAt: string; recordData: Record<string, unknown> };
type Envelope<T> = { code: number; message: string; data: T | null };
const typeLabel: Record<string, string> = { normal: "普通表单", workflow: "流程表单", detail: "明细表" };

export default function RecycleBinPage() {
  const searchParams = useSearchParams(); const formUuidFilter = searchParams.get("formUuid")?.trim() ?? "";
  const [items, setItems] = useState<Entry[]>([]); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState<string | null>(null); const [emptyConfirm, setEmptyConfirm] = useState(false); const [preview, setPreview] = useState<Entry | null>(null); const [previewSchema, setPreviewSchema] = useState<RuntimeFormSchema | null>(null); const [previewError, setPreviewError] = useState<string | null>(null); const [previewLoading, setPreviewLoading] = useState(false);
  const load = async () => { setLoading(true); try { const query = formUuidFilter ? `?formUuid=${encodeURIComponent(formUuidFilter)}` : ""; const response = await fetch(`/api/recycle-bin${query}`, { cache: "no-store" }); const body = await response.json() as Envelope<Entry[]>; if (!response.ok || body.code !== 0) throw new Error(body.message); setItems(body.data ?? []); } catch (error) { toast.danger("无法加载回收站", { description: error instanceof Error ? error.message : "请稍后重试" }); } finally { setLoading(false); } };
  // The timer postpones the initial request until after hydration and is rerun when the URL filter changes.
  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, [formUuidFilter]);
  const action = async (id: string, restore = false) => { if (!restore && !window.confirm("永久删除后无法恢复，确认继续吗？")) return; setBusy(id); try { const response = await fetch(`/api/recycle-bin/${id}${restore ? "/restore" : ""}`, { method: restore ? "POST" : "DELETE" }); const body = await response.json() as Envelope<unknown>; if (!response.ok || body.code !== 0) throw new Error(body.message); toast.success(restore ? "数据已恢复" : "数据已永久删除"); await load(); } catch (error) { toast.danger("操作失败", { description: error instanceof Error ? error.message : "请稍后重试" }); } finally { setBusy(null); } };
  const view = async (item: Entry) => { setPreview(item); setPreviewSchema(null); setPreviewError(null); setPreviewLoading(true); try { const result = await getFormSchema({ path: { formUuid: item.formUuid }, query: { scope: "published" }, responseStyle: "fields" }); const schema = result.data?.code === 0 ? result.data.data?.schema : null; if (result.error || !schema) throw new Error(result.data?.message ?? "原表单已删除或其 Schema 不可用"); setPreviewSchema(schema as RuntimeFormSchema); } catch (error) { setPreviewError(error instanceof Error ? error.message : "无法加载表单定义"); } finally { setPreviewLoading(false); } };
  const empty = async () => { setBusy("all"); try { const response = await fetch("/api/recycle-bin", { method: "DELETE" }); if (!response.ok) throw new Error("清空失败"); setEmptyConfirm(false); toast.success("回收站已清空"); await load(); } catch (error) { toast.danger("清空失败", { description: error instanceof Error ? error.message : "请稍后重试" }); } finally { setBusy(null); } };
  return <main className="theme-page-shell h-full overflow-auto"><section className="mx-auto w-full py-6"><div className="mb-5 flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-xl font-semibold text-[var(--color-text-primary)]">回收站</h1><p className="mt-1 text-sm text-[var(--color-text-secondary)]">已删除的表单数据会在保留期内显示在这里。</p></div><Button variant="secondary" className="text-[var(--color-danger)]" isDisabled={!items.length || busy !== null} onPress={() => setEmptyConfirm(true)}><TrashBin className="h-4 w-4" />清空回收站</Button></div><Table><Table.ScrollContainer className="overflow-x-auto"><Table.Content aria-label="回收站数据"><Table.Header><Table.Column id="source" isRowHeader>来源表单</Table.Column><Table.Column id="type">表单类型</Table.Column><Table.Column id="deleted">删除时间</Table.Column><Table.Column id="expires">到期时间</Table.Column><Table.Column id="actions">操作</Table.Column></Table.Header><Table.Body renderEmptyState={() => loading ? "加载中…" : "回收站暂无数据"}><Table.Collection items={items}>{(item) => <Table.Row key={item.id}><Table.Cell><div className="font-medium">{item.sourceFormName}</div><div className="text-xs text-[var(--color-text-secondary)]">{item.recordUuid}</div></Table.Cell><Table.Cell>{typeLabel[item.formType] ?? item.formType}</Table.Cell><Table.Cell>{new Date(item.deletedAt).toLocaleString("zh-CN")}</Table.Cell><Table.Cell>{new Date(item.expiresAt).toLocaleString("zh-CN")}</Table.Cell><Table.Cell><div className="flex gap-2"><Button size="sm" variant="secondary" isDisabled={busy !== null} onPress={() => void view(item)}><Eye className="h-4 w-4" />查看</Button><Button size="sm" variant="secondary" isDisabled={busy !== null} onPress={() => void action(item.id, true)}><ArrowRotateLeft className="h-4 w-4" />恢复</Button><Button size="sm" variant="secondary" className="bg-[var(--color-danger-soft)] text-[var(--color-danger)]" isDisabled={busy !== null} onPress={() => void action(item.id)}><TrashBin className="h-4 w-4" />永久删除</Button></div></Table.Cell></Table.Row>}</Table.Collection></Table.Body></Table.Content></Table.ScrollContainer></Table></section><Modal isOpen={emptyConfirm} onOpenChange={setEmptyConfirm}><Modal.Backdrop className="theme-modal-backdrop"><Modal.Container placement="center" size="sm"><Modal.Dialog className="rounded-md bg-[var(--color-bg-surface)]"><Modal.Header><Modal.Heading>清空回收站</Modal.Heading></Modal.Header><Modal.Body>所有回收站记录将永久删除，且无法恢复。</Modal.Body><Modal.Footer><Button variant="ghost" onPress={() => setEmptyConfirm(false)}>取消</Button><Button className="bg-[var(--color-danger)] text-white" isDisabled={busy === "all"} onPress={() => void empty()}>确认清空</Button></Modal.Footer></Modal.Dialog></Modal.Container></Modal.Backdrop></Modal><Drawer isOpen={preview !== null} onOpenChange={(open) => !open && setPreview(null)}><Drawer.Backdrop className="theme-modal-backdrop"><Drawer.Content placement="right"><Drawer.Dialog className="flex h-[100dvh] w-[min(960px,80vw)] max-w-[96vw] flex-col overflow-hidden border-l border-[var(--color-border)] bg-[var(--color-bg-surface)] text-[var(--color-text-primary)] shadow-[var(--shadow-dialog)]"><Drawer.Header className="shrink-0 border-b border-[var(--color-border)] px-5 py-4"><Drawer.Heading>{preview?.sourceFormName ?? "回收站数据"}</Drawer.Heading><Drawer.CloseTrigger aria-label="关闭" /></Drawer.Header><Drawer.Body className="min-h-0 flex-1 overflow-y-auto px-5 py-5">{previewLoading ? <p className="text-sm text-[var(--color-text-secondary)]">正在加载原始表单…</p> : previewError ? <p className="text-sm text-[var(--color-danger)]">{previewError}</p> : previewSchema && preview ? <RuntimeFormRenderer schema={previewSchema} formId={preview.formUuid} initialValues={preview.recordData} isReadOnly showSubmitButton={false} submitLabel="恢复数据" onSubmit={async () => undefined} /> : null}</Drawer.Body><Drawer.Footer className="shrink-0 justify-end gap-2 border-t border-[var(--color-border)] px-5 py-3"><Button variant="ghost" onPress={() => setPreview(null)}>关闭</Button><Button isDisabled={!preview || previewLoading || previewError !== null || busy !== null} onPress={() => { if (preview) { void action(preview.id, true); setPreview(null); } }}><ArrowRotateLeft className="h-4 w-4" />恢复数据</Button></Drawer.Footer></Drawer.Dialog></Drawer.Content></Drawer.Backdrop></Drawer></main>;
}
